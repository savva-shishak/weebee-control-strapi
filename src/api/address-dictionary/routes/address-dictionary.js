'use strict';

/**
 * address-dictionary router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::address-dictionary.address-dictionary');
